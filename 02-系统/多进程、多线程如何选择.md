   

# 多进程、多线程如何选择

## 为什么要采用多进程或多线程？

- 要并行处理多个任务，单进程无法完成
    
- 要获得更多的CPU资源，加快处理速度 --- 对于单核获得更多的时间片；对于多核，还能在更多的核上运行
    

  

  

## 内核线程模型

### 准确理解进程、线程

下面这段话讲，进程可以分成两个部分：一个用来执行的线程 + 一组进程内的资源。

![](多进程、多线程如何选择-20260911132028482.png)  

  

什么是线程呢？

假设你经营着一家物业管理公司。最初，业务量很小，事事都需要你亲力亲为。给老张家修完暖气管道，立马再去老李家换电灯泡——这叫单线程，所有的工作都得顺序执行。

后来业务拓展了，你雇佣了几个工人，这样，你的物业公司就可以同时为多户人家提供服务了——这叫多线程，你是主线程。

![](多进程、多线程如何选择-20260911132028482.jpg)  

工人们使用的工具，是物业管理公司提供的，这些工具由大家共享，并不专属于某一个人——这叫多线程资源共享。

工人们在工作中都需要管钳，可是管钳只有一把——这叫冲突。解决冲突的办法有很多，比如排队等候、等同事用完后的微信通知等——这叫线程同步。

你给工人布置任务——这叫创建线程。之后你还得要告诉他，可以开始了，不然他会一直停在那儿不动——这叫启动线程（start）。

如果某个工人（线程）的工作非常重要，你（主线程）也许会亲自监工一段时间，如果不指定时间，则表示你会一直监工到该项工作完成——这叫线程参与（join）。

业务不忙的时候，你就在办公室喝喝茶。下班时间一到，你群发微信，所有的工人不管手头的工作是否完成，都立马撂下工具，跟你走人。因此如果有必要，你得避免不要在工人正忙着的时候发下班的通知——这叫线程守护属性设置和管理（daemon）。

![](多进程、多线程如何选择-20260911132028483.jpg)  

再后来，你的公司规模扩大了，同时为很多生活社区服务，你在每个生活社区设置了分公司，分公司由分公司经理管理，运营机制和你的总公司几乎一模一样——这叫多进程，总公司叫主进程，分公司叫子进程。

总公司和分公司，以及各个分公司之间，工具都是独立的，不能借用、混用——这叫进程间不能共享资源。各个分公司之间可以通过专线电话联系——这叫管道。各个分公司之间还可以通过公司公告栏交换信息——这叫进程间共享内存。另外，各个分公司之间还有各种协同手段，以便完成更大规模的作业——这叫进程间同步。分公司可以跟着总公司一起下班，也可以把当天的工作全部做完之后再下班——这叫守护进程设置。

## 进程有什么用？

进程可以说是一个“执行中的程序”。程序是指令、数据及其组织形式的描述，是一个没有生命的实体，只有处理器赋予程序生命时（操作系统执行之），它才能成为一个活动的实体，我们称其为进程。

有了线程技术，我们就可以在一个进程中创建多个线程，让它们在“同一时刻”分别去做不同的工作了。这些线程共享同一块内存，线程之间可以共享对象、资源，如果有冲突或需要协同，还可以随时沟通以解决冲突或保持同步。

![](多进程、多线程如何选择-20260911132028483-1.jpg)  

不过，多线程技术不是万金油，它有一个致命的缺点：在一个进程内，不管你创建了多少线程，它们总是被限定在一颗CPU内，或者多核CPU的一个核内。这意味着，多线程在宏观上是并行的，在微观上则是分时切换串行的，多线程编程无法充分发挥多核计算资源的优势。这也是使用多线程做任务并行处理时，线程数量超过一定数值后，线程越多速度反倒越慢的原因。

![](多进程、多线程如何选择-20260911132028484.jpg)  

多进程技术正好弥补了多线程编程的不足，我们可以在每一颗CPU上，或者多核CPU的每一个核上启动一个进程，如果有必要，还可以在每个进程内再创建适量的线程，最大限度地使用计算资源解决问题。因为不在同一块内存区域内，和线程相比，进程间的资源共享、通信、同步等，都要麻烦得多，受到的限制也更多。

  

### 进程的状态

![](多进程、多线程如何选择-20260911132028484.png)  

### 内核线程模型

CPU 调度的只能是内核线程，CPU分配时间片的单元也是内核线程。

  

- 多对一模型：
    

用户级线程驻留在用户空间，运行时位于用户空间的库调度并分派这些线程，操作系统不可见用户级线程，因此他们无法被调度到处理器内核。

因此，就线程的同时执行而言，任意给定时刻每个进程只能够有一个线程在运行，而且只有一个处理器内核会被分配给该进程。

  

优点：

用户级线程仅存在于用户空间中，创建、撤销、线程之间的同步与通信功能，利用库调度器而无须利用系统调用来实现，代价比内核线程少得多，因为保存线程状态的过程和调用程序都只是本地过程。

用户进程利用线程库来控制用户线程。由于线程在进程内切换的规则远比进程调度和切换的规则简单，不需要用户态/核心态切换（不需要陷阱、上下文切换、更新内存高速缓存），切换速度快。

允许每个进程定制自己的线程调度算法，线程管理比较灵活。

线程能够利用的表空间和堆栈空间比内核级线程多。

  

缺点：

线程发生I/O或页面故障引起的阻塞时，由于内核不知道有多线程的存在，而会阻塞整个进程也就是阻塞所有线程。页面失效也会产生类似的问题。

一个单独的进程内部没有时钟中断，所以不可能用轮转调度的方式调度线程。

资源调度按照进程进行，多处理器系统中同一个进程中的线程也只能使用一个处理器，多用户线程在多处理器系统中不可以比在单处理器系统中获得更好的性能。

![](多进程、多线程如何选择-20260911132028485.png)  

  

  

-  一对一模型：
    

内核级线程驻留在内核空间，它们是内核对象。进程中存在多个用户线程，运行时库为每个用户级线程请求一个内核级线程。每个用户线程在其生命期内，会被映射或绑定到一个内核线程。一旦用户线程终止，两个线程都将离开系统。

  

优点：

如果进程中的一个线程被阻塞，能够切换同一进程内的其他线程继续执行

多处理器系统中，内核能够并行执行同一进程内的多个线程

  

缺点：

所有能够阻塞线程的调用都以系统调用的形式实现，代价可观

操作系统的内存管理和调度子系统必须要考虑到数量巨大的用户级线程，用户必须了解每个进程允许的线程的最大数目是多少。

  

![](多进程、多线程如何选择-20260911132028485-1.png)  

  

- 多对多模型：
    

  

混合线程实现是用户线程和内核线程的交叉，使得库和操作系统都可以管理线程。

用户线程由运行时库调度器管理，内核线程由操作系统调度器管理。

可运行的用户线程由运行时库分派并标记为准备好执行的可用线程；

操作系统选择用户线程并将它映射到可用内核线程（多个用户线程可以分配给相同的内核线程）。该方法中尽量使用多对一映射，很多用户线程将会映射到一个内核线程。因此，对内核线程的请求将会少于用户线程的数目。

  

![](多进程、多线程如何选择-20260911132028486.png)  

  

  

## 选择时考虑的因素

- 是不是存在多任务并发的需求
    

  

- 需不需要通过多进程、多线程来提升处理任务的速度
    

  

- 计算密集型、IO密集型
    

计算密集型任务的特点是要进行大量的计算，消耗CPU资源，比如计算圆周率、对视频进行高清解码等等，全靠CPU的运算能力。这种计算密集型任务虽然也可以用多任务完成，但是任务越多，花在任务切换的时间就越多，CPU执行任务的效率就越低，所以，要最高效地利用CPU，计算密集型任务同时进行的数量应当等于CPU的核心数。--- 假设没有其他任务进程的情况下

  

IO密集型，涉及到网络、磁盘IO的任务都是IO密集型任务，这类任务的特点是CPU消耗很少，任务的大部分时间都在等待IO操作完成（因为IO的速度远远低于CPU和内存的速度）。对于IO密集型任务，任务越多，CPU效率越高，但也有一个限度。常见的大部分任务都是IO密集型任务，比如Web应用。 --- 我的目的不是提升CPU效率，而是要提升处理性能，这里本末倒置了。

  

- 模块管理
    

多进程，某个进程挂了，不影响其他进程，直接拉起来就行。

多线程，会导致其他线程也挂掉

  

- 软件开发管理
    

多进程，可以让不同的人开发

多线程，就很难拆分了

  

- 同步与通信
    

进程之间通信，需要IPC

线程之间，线程间同步、通信，效率高，因为共享同一片进程空间

  

- 运行调度
    

进程创建开销大，进程切换开销大

线程创建开销小，线程切换开销小

  

  

## 场景分析

### owt 采用多进程 + epoll多路复用

owt 采用epoll来接收网络数据，其实并没有多任务并发的需求

  

owt 采用多进程/多线程，就是为了提升处理网络数据的带宽性能

  

owt 面临的主要还是一个 IO 密集型的任务，主要还是要加快从网卡取数据，解决方法是用一个比较空闲的核来处理网卡中断。

  

  

## 其他问题

- 这是不是就意味着尽量使用多线程并发，这样可以抢到更多的时间片。
    

理论上是的，多线程的一种用途就是能同时做好几件事情，以提高效率。但实际问题是，CPU的数量（核心数，下同）是有限的，而且并不多。如果你的CPU有8个CPU，并且整个系统中有8个线程的话，不考虑中断等因素，每个线程理论上能一直执行下去。然而多于8个线程以后，操作系统就必须进行调度，也就是分配时间片。具体的分配方案，或者说调度算法有很多种，详情参见[Scheduling (computing)](https://link.zhihu.com/?target=https%3A//en.wikipedia.org/wiki/Scheduling_%28computing%29)。如果一个进程创建了很多线程的话，最多也只有8个能够处于执行的状态[2]，其余的线程必须等待调度。线程被调度的时候需要进行上下文切换，这个操作是一种额外的开销。线程数量过多的时候，上下文切换产生的额外开销会对系统的效率造成负面影响。

- 一个多核cpu机器上，一个进程内的所有线程都在一个核心上跑吗，还是可以同时在多核上运行？
    

如果没做特殊设置，可以在任意核心上运行。一个进程内的多个线程，可以同时在多个核心上跑。切换线程时，可能是同一进程的线程，也可能是另一个进程的线程，如果是其他进程的线程，需要切换进程上下文。

- 多线程中，一个线程崩溃，为什么导致进程崩溃
    

严格的说没有“线程崩溃”，只是触发了SIGSEGV (Segmentation Violation/Fault)。如果没有设置对应的Signal Handler操作系统就自动终止进程（或者说默认的Signal Handler就是终止进程）；如果设置了，理论上可以恢复进程状态继续跑（用longjmp之类的工具）